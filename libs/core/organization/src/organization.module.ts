/**
 * @description Organization组织管理模块
 * @author 技术架构师
 * @since 2.1.0
 */

import { Module } from '@nestjs/common';

@Module({
  imports: [
    // 依赖模块导入
  ],
  controllers: [
    // 控制器
  ],
  providers: [
    // 服务提供者
  ],
  exports: [
    // 导出服务
  ],
})
export class OrganizationModule {}
